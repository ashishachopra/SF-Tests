# =============================================================================
# R Only 
# =============================================================================
# Usage: sfnb-setup install --config configs/r_only.yaml
# =============================================================================

env_name: "workspace_env"

languages:
  r:
    enabled: true
    r_version: "4.5.2"   # pin to known-good conda-forge R; bump when packages are rebuilt
    conda_packages:
      - r-tidyverse
      - r-dbplyr
      - r-httr2
      - r-lazyeval
      - r-reticulate>=1.25
    cran_packages: []
    addons:
      adbc: true
      duckdb: true

  scala:
    enabled: false

  julia:
    enabled: false
